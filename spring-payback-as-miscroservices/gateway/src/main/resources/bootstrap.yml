spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://registry1:9009/eureka/
