info:

  some:
    property:
      visible:
        everywhere: value

  same:
    property:
      defined:
        everywhere: no profile

customerProps:
  firstNameSuffix: suffix to be added

server:
  port: 9002
